{
    "apt_packages": [
        "vim",
        "jq",
        "zsh",
        "htop",
        "tree"
    ],
    "pip_packages": [
        "black",
        "pylint",
        "pytest",
        "python-dotenv"
    ],
    "npm_packages": [
        "typescript",
        "ts-node"
    ],
    "custom_tools": [
        {
            "name": "terraform",
            "version": "1.6.6",
            "install_command": "curl -fsSL https://releases.hashicorp.com/terraform/${version}/terraform_${version}_linux_amd64.zip -o terraform.zip && unzip terraform.zip && mv terraform /usr/local/bin/ && rm terraform.zip"
        }
    ]
}